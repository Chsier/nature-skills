[project]
name = "nature-skills"
version = "0.1.0"
description = "Local development environment for the nature-skills repository."
readme = "README.md"
requires-python = "==3.12.12"
dependencies = []

[tool.uv]
package = false
